pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checking out code from Git...'
                git branch: 'main', url: 'https://github.com/Nighedhana09/docker-jenk.git'
            }
        }

        stage('Clean old containers') {
            steps {
                echo 'Stopping and removing old containers if they exist...'
                sh 'docker-compose -f docker-compose.yaml down || true'
            }
        }

        stage('Build Docker Images') {
            steps {
                echo 'Building Docker images using docker-compose...'
                sh 'docker-compose -f docker-compose.yaml build'
            }
        }

        stage('Start Containers') {
            steps {
                echo 'Starting containers using docker-compose...'
                sh 'docker-compose -f docker-compose.yaml up -d'
            }
        }

        stage('SonarQube Analysis') {
            steps {
                echo 'Running SonarQube scan...'
                // 'SonarQube' is the name of the server you configured in Jenkins
                withSonarQubeEnv('SonarQube') {
                    sh 'sonar-scanner -Dsonar.projectKey=docker-jenk -Dsonar.sources=./'
                }
            }
        }
    }

    post {
        success {
            echo '✅ Build and deployment completed successfully.'
        }
        failure {
            echo '❌ Build failed. Please check logs.'
        }
    }
}
